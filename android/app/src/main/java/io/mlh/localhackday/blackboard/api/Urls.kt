package io.mlh.localhackday.blackboard.api

class Urls {
    companion object {
        private const val URL_PROTOCOL = "https://"
        private const val DOMAIN = "mlh.pushkaranand.me"
        const  val API_BASE_URL = "$URL_PROTOCOL$DOMAIN"
    }
}